- job-template:
   name: '{name}-flake8'
   disabled: false

   builders:
    - flake8:
       project: $WORKSPACE

   scm:
    - git:
       url: '${{ZUUL_URL}}/${{ZUUL_PROJECT}}'
       branches:
        - '${{ZUUL_COMMIT}}'
       refspec: '${{ZUUL_REF}}'
       clean: true