- job-template:
   name: '{name}-puppet-checks'
   disabled: false

   scm:
    - git:
       url: '${{ZUUL_URL}}/${{ZUUL_PROJECT}}'
       branches:
        - '${{ZUUL_COMMIT}}'
       refspec: '${{ZUUL_REF}}'
       clean: true

   builders:
   - puppet-lint-multi:
   - puppet-syntax:

